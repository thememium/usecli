[tool.usecli]
title = "{{ title | default('My CLI') }}"
description = "{{ description | default('A custom CLI tool') }}"
show_setup = {{ show_setup | default(true) | lower }}
commands_dir = "{{ commands_dir | default('commands') }}"
